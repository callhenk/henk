# Voice & Telephony Standards

## Twilio Integration

- Handle Twilio webhook events properly
- Implement proper error handling for voice calls
- Use environment variables for all Twilio credentials
- Implement call status tracking and logging

## ElevenLabs Integration

- Use ElevenLabs voice cloning responsibly
- Implement proper audio streaming
- Handle API rate limits and quotas
- Use appropriate voice models for different use cases

## Webhook Handling

```typescript
// Handle Twilio webhook events
export async function POST(request: Request) {
  const formData = await request.formData();
  const callStatus = formData.get("CallStatus");
  const callSid = formData.get("CallSid");

  // Process call status and update database
  await updateCallStatus(callSid, callStatus);

  return new Response("OK", { status: 200 });
}
```

## Voice Generation

- Pre-generate audio when possible for better performance
- Implement proper caching for voice assets
- Handle real-time voice generation for dynamic content
- Monitor ElevenLabs API usage and costs

## Compliance

- Implement call recording compliance
- Handle DTMF input properly
- Follow telephony regulations
- Implement proper consent tracking for voice calls
  description:
  globs:
  alwaysApply: false

---
